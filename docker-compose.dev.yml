version: "3.8"

services:
  app:
    container_name: glassradar-client-dev
    image: glassradar-client-dev
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000
